import { PhaseStub } from "@/components/phase-stub";

export default function PitchesPage() {
  return (
    <PhaseStub
      n={3}
      title="Pitches"
      description="AI pitch composer. Generate personalized drafts per contact, batch-generate across your target list, and pick from A/B tone variants."
      chunk="Chunk F"
    />
  );
}
