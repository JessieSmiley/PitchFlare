import { PhaseStub } from "@/components/phase-stub";

export default function SocialPostsPage() {
  return (
    <PhaseStub
      n={3}
      title="Social Posts"
      description="Per-platform posts (X, LinkedIn, Instagram, Threads) with length/format appropriate to each channel."
      chunk="Chunk F"
    />
  );
}
