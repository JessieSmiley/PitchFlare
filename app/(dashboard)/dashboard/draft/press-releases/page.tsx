import { PhaseStub } from "@/components/phase-stub";

export default function PressReleasesPage() {
  return (
    <PhaseStub
      n={3}
      title="Press Releases"
      description="Draft press releases with headline, subhead, body, dateline, and brand boilerplate. AP-style checks included."
      chunk="Chunk F"
    />
  );
}
