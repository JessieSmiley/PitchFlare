import { PhaseStub } from "@/components/phase-stub";

export default function LevelSetPage() {
  return (
    <PhaseStub
      n={1}
      title="Level-Set"
      description="Establish your brand so every AI output downstream is grounded: voice, boilerplate, messaging pillars, spokespeople, and prior-work examples."
      chunk="Chunk D"
    />
  );
}
