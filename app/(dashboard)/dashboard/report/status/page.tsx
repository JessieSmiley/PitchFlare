import { PhaseStub } from "@/components/phase-stub";

export default function StatusReportPage() {
  return (
    <PhaseStub
      n={6}
      title="Status Reports"
      description="On-demand narrative summary of what happened on this campaign — sent, opened, replied, placed, next actions."
      chunk="Chunk G"
    />
  );
}
