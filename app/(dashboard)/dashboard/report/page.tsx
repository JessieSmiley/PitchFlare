import { PhaseStub } from "@/components/phase-stub";

export default function ReportPage() {
  return (
    <PhaseStub
      n={6}
      title="Report"
      description="Coverage reports, ROI, Share of Voice, and on-demand status briefs. Every report is shareable and exportable as branded PDF."
      chunk="Chunk G"
    />
  );
}
