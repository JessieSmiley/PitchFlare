import { PhaseStub } from "@/components/phase-stub";

export default function BillingPage() {
  return (
    <PhaseStub
      title="Billing"
      description="Current plan, seat + brand usage vs. limits, and Stripe Checkout / Customer Portal links."
      chunk="Chunk H"
    />
  );
}
