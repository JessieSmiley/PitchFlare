import { PhaseStub } from "@/components/phase-stub";

export default function AnalyzePage() {
  return (
    <PhaseStub
      n={5}
      title="Analyze"
      description="Coverage feed, sentiment, share of voice, estimated reach. Auto-monitors keywords via Google News RSS + GDELT + Bing News every 6 hours."
      chunk="Chunk G"
    />
  );
}
