import { PhaseStub } from "@/components/phase-stub";

export default function MonitoringPage() {
  return (
    <PhaseStub
      n={5}
      title="Monitoring"
      description="Live feed of brand + campaign keyword hits across news, podcasts, and social. Promote raw mentions into tracked coverage clips."
      chunk="Chunk G"
    />
  );
}
