import { PhaseStub } from "@/components/phase-stub";

export default function IdeationPage() {
  return (
    <PhaseStub
      n={2}
      title="Ideation Station"
      description="AI angle generator: enter a news hook or launch and Claude returns ranked story angles with audience fit, newsworthiness score, and media-tier fit."
      chunk="Chunk E"
    />
  );
}
