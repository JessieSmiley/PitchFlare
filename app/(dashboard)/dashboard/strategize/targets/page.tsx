import { PhaseStub } from "@/components/phase-stub";

export default function TargetsPage() {
  return (
    <PhaseStub
      n={2}
      title="Target Compilation"
      description="Search the shared contact directory (journalists, podcasters, influencers), rank matches against your current angle, and build a MediaList for this campaign."
      chunk="Chunk E"
    />
  );
}
